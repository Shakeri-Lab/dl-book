# Linux independent-repeat diagnosis — September 5, 2026

This is a rejected-candidate diagnostic, not a canonical acceptance receipt.
Neither original status nor any frozen artifact was rewritten. No portability
tolerance has changed, and the published v1.3 artifacts remain untouched.

## Evidence and result

The first run, [33937390845](https://github.com/Shakeri-Lab/dl-book/actions/runs/33937390845),
and its independent saved-image repeat,
[33938819036](https://github.com/Shakeri-Lab/dl-book/actions/runs/33938819036),
executed source `1da26e860d6fa76ae6b76dede273748b1439128c`. Both failed the old
silent-cell coverage checker after execution. Their raw diagnostic bundles live
under `build/canonical-1da26e8-<run-id>/`; the independent read-only comparison is
`build/diagnostic-linux-a-repeat/diagnostic.json`, SHA-256
`0c980c66732aafe40fff64a923b53c3e411022abf41759244a7490aa7a1a993c`.
Its script is `build/diagnostic-linux-a-repeat/compare_rejected_runs.py`, SHA-256
`bcbc11a6b783b833471dd1d954119fd4eee94894ff308be36872a5fffa49abe5`.
Every quantitative observation in this section comes from that report and the
original artifact paths and hashes it inventories.

All 133 ordered stdout blocks agree across both independent runs and both
formats. All eight raw paired-study sidecars agree byte for byte. The complete
freeze does not: 11 of 274 files differ.

| Surface | Difference | Interpretation |
|---|---|---|
| Chapter 1 HTML and LaTeX result JSON | Displayed `torch.Generator` memory address | Incidental process identity, not a numerical measurement |
| Five other HTML result JSONs | Generated cell IDs in the three interludes and Chapters 18–19 | Unlabeled cells acquire random Quarto IDs |
| Chapter 1 three-method fit/loss PDFs | Drawing coordinates differ in the PDF content stream | Genuine figure bytes, not timestamp-only churn |
| Chapter 4 SGD-zone PNG/PDF | Drawing coordinates and two raster pixels differ | Genuine figure bytes; largest changed channel differs by one |

Source archive, recorded input hashes, saved-image ID, 68 wheel records, and all
54 startup policies per run agree. Both use one intra-op and one inter-op thread.
The old records omit physical CPU model and flags; different hosted workers and
regions do not establish different CPUs or a CPU cause. The image archive has
not yet been independently rehashed in the local comparison. The diagnostic
workflow must verify it before loading the saved image.

## Corrections and discriminating probe

Suppress the uninformative returned Generator display without changing its seed
call. Give every authored native cell a stable label, including currently silent
cells. Keep public figure labels unchanged. A real-Quarto pretraining witness
must compare complete generated bytes across two independent tiny projects, not
only their printed values.

Do not round or normalize frozen figure files to make them pass. First test the
two small least-squares inputs behind the changed figures in the exact saved
image, without training. The diagnostic source and the older image source have
separate identities. Preserve the original image verifier, archive checksum,
loaded image ID, CPU/flags, library configuration, and actual environment.

The ranked hypotheses and their predictions are:

1. Repeated least-squares calls vary despite identical input bytes. Repeated
   solves inside one process will expose this; alternative drivers are separate
   controlled comparisons, not presumed fixes.
2. A fresh process or numerical dispatch path changes the result. Fresh-process
   repeats with otherwise fixed inputs/policy will differ, while a within-process
   repeat can remain stable. Record tensor layout/alignment rather than assuming
   it is immaterial.
3. Plot export varies despite fixed coordinates. A plot-only control with exact
   coordinates, authored labels, and a fixed source epoch will expose this.

The proposed dispatch variants are diagnostics only. A finite successful probe
does not guarantee universal host independence. Any source or runtime correction
still needs two new complete same-source, exact-image executions with the original
all-file requirement before promotion.

The `98349c8` candidate and both accompanying Mac profiles were cancelled when
this full-file discrepancy was discovered. Their partial records stay rejected;
there is no second `98349c8` independent run to dispatch. Finish the minimal
diagnosis and corrections before starting another full execution cycle.
