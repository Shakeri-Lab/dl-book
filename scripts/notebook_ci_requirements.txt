# Tooling used to export and validate the public notebook artifacts in CI.
# Keep these separate from notebook_requirements.txt, which pins learner runtime.
nbformat==5.10.4
nbclient==0.11.0
nbconvert==7.17.1
ipykernel==7.3.0
# Decode raster outputs and parse SVG safely in the figure audit.
Pillow==12.3.0
defusedxml==0.7.1
