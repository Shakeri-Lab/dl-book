\begin{tikzpicture}[
  >=Stealth,
  every node/.style={font=\small},
  note/.style={font=\footnotesize, align=center},
  entry/.style={inner sep=2pt, minimum width=0.72cm, minimum height=0.48cm}
]
  \matrix (X) [
    matrix of math nodes,
    nodes={entry, text=dlblue},
    row sep=0.16cm,
    column sep=0.34cm,
    left delimiter={[},
    right delimiter={]}
  ] {
    x_{11} & x_{12} & \cdots & x_{1d}\\
    x_{21} & x_{22} & \cdots & x_{2d}\\
    \vdots & \vdots & \ddots & \vdots\\
    x_{n1} & x_{n2} & \cdots & x_{nd}\\
  };
  \node[dlblue, font=\large, left=0.38cm of X] (Xlabel)
    {$\mathbf{X}=$};

  \matrix (y) [
    matrix of math nodes,
    nodes={entry, text=dlpurple},
    row sep=0.16cm,
    left delimiter={[},
    right delimiter={]},
    right=2.25cm of X
  ] {
    y_1\\y_2\\\vdots\\y_n\\
  };
  \node[dlpurple, font=\large, left=0.38cm of y] (ylabel)
    {$\mathbf{y}=$};

  \begin{scope}[on background layer]
    \node[fit=(X-1-1)(X-1-4), fill=dlblue!11, rounded corners=2pt,
      inner xsep=5pt, inner ysep=3pt] {};
    \node[fit=(y-1-1), fill=dlpurple!13, rounded corners=2pt,
      inner xsep=5pt, inner ysep=3pt] {};
  \end{scope}

  \node[note, dlblue, above=0.78cm of X-1-2] (rowlabel)
    {one input $\mathbf{x}_1^\top$};
  \draw[-{Stealth}, thick, dlblue]
    (rowlabel.south) -- ($(X-1-2.north)+(0,0.05)$);

  \draw[densely dashed, thick, gray!65]
    ($(X-1-4.east)+(0.08,0)$) -- ($(y-1-1.west)+(-0.08,0)$)
    node[midway, above=2pt, note, text=gray!75] {same example};

  \draw[{Stealth}-{Stealth}, dlblue]
    ($(X.south west)+(0,-0.40)$) -- ($(X.south east)+(0,-0.40)$)
    node[midway, below=3pt, note] {$d$ features};
  \draw[{Stealth}-{Stealth}, dlblue]
    ($(X.north west)+(-1.55,0)$) -- ($(X.south west)+(-1.55,0)$)
    node[midway, left=4pt, note] {$n$ examples};
\end{tikzpicture}
