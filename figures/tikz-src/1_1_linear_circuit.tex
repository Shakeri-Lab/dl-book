\begin{tikzpicture}[
  >=Stealth,
  every node/.style={font=\small},
  input/.style={
    draw=dlblue, fill=dlblue!7, rounded corners=2pt,
    minimum width=0.72cm, minimum height=0.52cm, text=dlblue
  },
  multiplier/.style={
    draw=dlorange, fill=dlorange!7, rounded corners=2pt,
    minimum width=1.15cm, minimum height=0.52cm, text=dlorange
  },
  sum/.style={
    circle, draw=gray!70, fill=gray!6, very thick,
    minimum size=0.88cm, inner sep=0pt
  }
]
  \node[input] (x1) at (0,1.25) {$x_1$};
  \node[input] (x2) at (0,0.25) {$x_2$};
  \node[font=\large, text=gray!65] at (0,-0.48) {$\vdots$};
  \node[input] (xd) at (0,-1.25) {$x_d$};

  \node[multiplier, right=1.05cm of x1] (w1) {$\times\,w_1$};
  \node[multiplier, right=1.05cm of x2] (w2) {$\times\,w_2$};
  \node[multiplier, right=1.05cm of xd] (wd) {$\times\,w_d$};

  \node[sum] (sum) at (4.35,0) {$\Sigma$};
  \node[multiplier, above=0.82cm of sum] (bias) {$+\,b$};
  \node[
    draw=dlgreen, fill=dlgreen!7, rounded corners=2pt,
    minimum width=1.05cm, minimum height=0.58cm,
    right=1.18cm of sum, text=dlgreen, font=\bfseries
  ] (out) {$\hat y$};

  \foreach \src/\mul in {x1/w1,x2/w2,xd/wd} {
    \draw[->, thick, dlblue!75] (\src) -- (\mul);
    \draw[->, thick, gray!70] (\mul) -- (sum);
  }
  \draw[->, thick, dlorange] (bias) -- (sum);
  \draw[->, very thick, dlgreen] (sum) -- (out);

  \node[font=\footnotesize, text=dlblue, below=0.22cm of xd]
    {inputs};
  \node[font=\footnotesize, text=dlorange, below=0.22cm of wd]
    {learnable weights};
  \node[font=\footnotesize, align=center, text=gray!75, below=0.63cm of sum]
    {add the weighted\\signals and bias};
  \node[font=\footnotesize, text=dlgreen, below=0.22cm of out]
    {model output};
\end{tikzpicture}
