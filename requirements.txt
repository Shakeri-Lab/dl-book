# Book execution environment — CPU-only; each cell should stay under about 4–5 min.
torch>=2.4
torchvision>=0.19
numpy>=1.26
matplotlib>=3.8
jupyter
scikit-learn>=1.4   # small datasets + baselines only (no modeling shortcuts in chapters)
pandas>=2.0
gdown>=5.0          # scripts/fetch_notebooks.py (Colab provenance export)
